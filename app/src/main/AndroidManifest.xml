<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginActivity">

        <!-- SignUpActivity không phải màn hình khởi động -->
        <activity
            android:name=".SignUpActivity"
            android:exported="false" />

        <!-- LoginActivity BÂY GIỜ LÀ MÀN HÌNH KHỞI ĐỘNG -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <!-- TẤM BIỂN "CỬA VÀO" PHẢI NẰM Ở ĐÂY -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity không còn là màn hình khởi động nữa -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>

</manifest>